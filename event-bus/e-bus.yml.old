apiVersion: v1
kind: Pod
metadata:
  name: event-bus
  labels:
    name: event-bus
spec:
  containers:
    - name: event-bus
      image: event-bus
      resources:
        limits:
          memory: "128Mi"
          cpu: "500m"
